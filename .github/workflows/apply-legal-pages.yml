name: Apply legal pages

on:
  push:
    branches: [codex/herostar-legal-pages]

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/herostar-legal-pages
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node .github/scripts/apply-legal-pages.mjs
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Добавить юридические страницы и защиту приёма оплаты"
          git push origin HEAD:codex/herostar-legal-pages
