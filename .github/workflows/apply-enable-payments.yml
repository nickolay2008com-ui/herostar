name: Apply enable payments

on:
  push:
    branches: [codex/herostar-enable-payments]

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/herostar-enable-payments
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node .github/scripts/apply-enable-payments.mjs
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Включить платежную кнопку и создание платежей ЮKassa"
          git push origin HEAD:codex/herostar-enable-payments
